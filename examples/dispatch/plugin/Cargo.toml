[package]
name = "dispatch-plugin"
version = "0.1.0"
edition = "2024"
publish = false
description = "Hands report delivery back to its host."

[lib]
crate-type = ["cdylib"]

[dependencies]
lockgate-plugin = { version = "0.1.0", path = "../../../crates/lockgate-plugin" }

[workspace]
