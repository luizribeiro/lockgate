[package]
name = "lockgate-demo"
version = "0.1.0"
edition = "2024"
publish = false
build = "build.rs"

[dependencies]
anyhow = "1.0.100"
lockgate = { path = "../.." }
wasmtime = { version = "47.0.3", features = ["component-model"] }
