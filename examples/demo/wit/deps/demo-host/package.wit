// The application-owned contract for host-visible plugin roles and host services.
package demo:host@0.1.0;

interface services {
  log: async func(message: string);
}

interface runnable {
  run: async func() -> result<string, string>;
}

interface file-reader {
  read: async func(path: string) -> result<string, string>;
}

world runnable-plugin {
  import services;
  export runnable;
}

world file-reader-plugin {
  import services;
  export file-reader;
}
