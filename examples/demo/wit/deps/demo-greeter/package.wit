// The canonical, versioned contract shared by greeter and its consumers.
package demo:greeter@0.1.0;

interface greeter {
  greet: func(name: string) -> string;
}

world greeter-plugin {
  export greeter;
}
