[workspace]
resolver = "3"
members = ["inkling-provider", "list-files", "read-file"]

[workspace.dependencies]
lockgate-plugin = { path = "../../../lockgate-plugin" }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"
