[package]
name = "audit-host"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
lockgate = { path = "../../../crates/lockgate" }
tokio = { workspace = true, features = ["macros", "rt"] }
