[package]
name = "audit-plugin"
version = "0.1.0"
edition = "2024"
publish = false
description = "Reports task progress to its host's audit log."

[lib]
crate-type = ["cdylib"]

[dependencies]
lockgate-plugin = { version = "0.1.0", path = "../../../crates/lockgate-plugin" }

[workspace]
