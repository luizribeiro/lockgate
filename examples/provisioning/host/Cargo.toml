[package]
name = "provisioning-host"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
lockgate = { version = "0.1.0", path = "../../../crates/lockgate" }
provisioning-policy = { version = "0.1.0", path = "../policy" }
tokio = { workspace = true, features = ["macros", "rt"] }
