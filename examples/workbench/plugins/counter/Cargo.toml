[package]
name = "counter-plugin"
version = "0.1.0"
edition = "2024"
publish = false
description = "Measures text with a word count."

[lib]
crate-type = ["cdylib"]

[dependencies]
lockgate-plugin = { version = "0.1.0", path = "../../../../crates/lockgate-plugin" }

[workspace]
