[package]
name = "greeter-plugin"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
lockgate-plugin = { path = "../../../crates/lockgate-plugin" }

[workspace]
