[package]
name = "lockgate-no-std-policy-reexport-fixture"
version = "0.0.0"
edition = "2024"

[workspace]

[dependencies]
policy = { package = "lockgate-policy", version = "0.1.0", path = "../../../../lockgate-policy" }
