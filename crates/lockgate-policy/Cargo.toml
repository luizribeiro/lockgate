[package]
name = "lockgate-policy"
version.workspace = true
edition.workspace = true

[dependencies]
lockgate-macros = { path = "../lockgate-macros" }
url.workspace = true

[dev-dependencies]
lockgate-schema = { path = "../lockgate-schema" }
proptest = "1"
trybuild = "1"
