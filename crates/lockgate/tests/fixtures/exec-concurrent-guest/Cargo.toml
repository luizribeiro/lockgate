[package]
name = "lockgate-exec-concurrent-fixture"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
dlmalloc.workspace = true
futures = { workspace = true, features = ["async-await"] }
wit-bindgen = { workspace = true, features = ["async", "macros"] }
