[package]
name = "lockgate-guarded-bindings-fixture"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
lockgate-plugin = { version = "0.1.0", path = "../../../../lockgate-plugin" }
