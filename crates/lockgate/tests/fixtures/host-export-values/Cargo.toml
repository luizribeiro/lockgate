[package]
name = "lockgate-host-export-values-fixture"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib"]

[dependencies]
lg = { package = "lockgate", version = "0.1.0", path = "../../../../lockgate" }
