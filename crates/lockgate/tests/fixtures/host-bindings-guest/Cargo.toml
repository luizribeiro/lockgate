[package]
name = "lockgate-host-bindings-fixture"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
futures = { workspace = true, features = ["async-await"] }
lockgate-plugin = { path = "../../../../lockgate-plugin" }
