package test:http-client;

interface guest {
  record response {
    status: u16,
    body: string,
  }

  get: async func(url: string) -> result<response, string>;
  get-with-first-byte-timeout: async func(url: string, timeout-millis: u64) -> result<response, string>;
  get-with-timeouts: async func(url: string, timeout-millis: u64) -> result<response, string>;
}

world fixture {
  export guest;
}
