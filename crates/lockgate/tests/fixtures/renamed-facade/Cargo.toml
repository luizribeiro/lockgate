[package]
name = "lockgate-renamed-facade-fixture"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib"]

[dependencies]
dlmalloc.workspace = true
lgp = { package = "lockgate-plugin", version = "0.1.0", path = "../../../../lockgate-plugin", default-features = false }
