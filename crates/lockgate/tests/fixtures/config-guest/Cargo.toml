[package]
name = "lockgate-config-fixture"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
dlmalloc.workspace = true
wit-bindgen = { workspace = true, features = ["macros"] }
