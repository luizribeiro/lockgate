package test:exec;

interface host {
  wait: async func();
}

interface guest {
  value: func() -> u32;
  suspend: async func() -> u32;
  trap: func();
  import-then-trap: async func();
}

world fixture {
  import host;
  export guest;
}
