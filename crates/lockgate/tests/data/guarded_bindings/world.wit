package test:guarded@1.2.3;

interface vm {
  create: func(pool: string) -> string;
  exec: func(vm: string, command: string) -> string;
  list-pools: func() -> list<string>;
  protocol-version: func() -> string;
}

interface admin {
  restart: func();
  status: func();
}

world fixture {
  import vm;
  import admin;
}

world vm-only {
  import vm;
}
